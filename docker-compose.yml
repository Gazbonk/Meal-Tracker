services:
  app:
    build: .
    image: hornsby-meal-tracker:latest
    container_name: hornsby-meal-tracker
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
